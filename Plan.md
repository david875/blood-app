# 血壓管理 App 佈景主題切換與持久化計劃

## 1. 需求概述
本專案的目標是在現有血壓管理 App 中加入 dark/light 兩種佈景主題，並在首頁上方提供切換開關。使用者選擇的佈景主題將以 localStorage 儲存，讓頁面重新載入後能自動恢復上一個主題設定。

## 2. 確認的需求細節
- 預設主題：light
- 儲存方式：localStorage
- 切換位置：首頁右上角
- 主題風格：高對比 dark + 清亮 light
- 操作模式：切換後立即套用，且全頁同步
- 目標範圍：僅涵蓋前端主題切換與持久化

## 3. 實作重點
### 3.1 主題管理方式
採用 CSS 變數方式管理主題，讓 dark/light 兩種顏色系統集中管理，維護簡單且容易擴充。

建議的切換方式：
- 在根節點加入 data-theme 屬性
- 例如：document.documentElement.dataset.theme = 'dark'
- 透過 CSS 變數統一控制背景、文字、卡片、按鈕與邊框顏色

### 3.2 佈景主題設定
light 主題：
- 背景偏白或淡藍
- 文字深色，提升可讀性
- 面板與卡片保持明亮清晰

dark 主題：
- 背景偏深色
- 文字偏淺色
- 面板與輸入框要有足夠對比度
- 按鈕與標籤仍需清楚可辨識

## 4. 主要檔案修改範圍
### index.html
- 在頁面上方加入主題切換開關
- 一般放置於首頁右上角
- 可使用按鈕或開關式元件實作

### styles.css
- 定義 light 和 dark 兩組 CSS 變數
- 將原本固定色值逐步改成變數引用
- 增加手機版與桌面版的美觀與可讀性調整

### script.js
- 初始化時讀取 localStorage 中保存的 theme
- 依照設定套用對應主題
- 當使用者點擊切換按鈕時，更新 theme 並寫入 localStorage
- 若重新整理頁面，會自動恢復上次的選擇

## 5. 實作步驟
1. 檢查現有首頁與 CSS 結構
2. 在首頁上方新增主題切換開關
3. 撰寫 light/dark 兩套 CSS 變數
4. 將現有樣式替換為變數引用
5. 在 JavaScript 中新增初始化與切換邏輯
6. 儲存使用者主題設定到 localStorage
7. 測試切換效果與頁面重整後是否保持設定
8. 檢查手機版與桌面版是否都能正常操作

## 6. 驗證條件
完成後需確認以下事項：
1. 頁面右上角可正常看到主題切換開關
2. 點選 dark/light 後，頁面顏色立即變更
3. 刷新頁面後，仍依照上一次選擇的佈景主題顯示
4. 手機版排版不會擠壓、遮擋或難以使用
5. localStorage 中的值確實被寫入且正確讀取

## 7. 實作範圍與限制
- 僅處理目前的前端頁面佈景主題切換
- 不含後端資料儲存或多頁同步邏輯
- 先做穩定、簡單且維護性高的版本

## 8. 未來擴充建議
如果後續功能再增加，可以考慮：
- 自動跟隨系統色彩設定（prefers-color-scheme）
- 更平滑的切換動畫
- 依據血壓數值顯示不同狀態顏色
- 擴充至多頁面共用主題設定

## 9. 結論
本計劃將以最小且穩定的前端方式為血壓管理 App 新增 dark/light 主題切換功能，並保留使用者偏好，讓 App 在重新開啟或重新整理後仍維持一致的外觀與體驗。
